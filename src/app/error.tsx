"use client";
import ErrorMessage from "@/components/ErrorMessage";

const Error = () => {
  return (
    <ErrorMessage message="500 Error" details="Unexpected error occured!" />
  );
};

export default Error;
